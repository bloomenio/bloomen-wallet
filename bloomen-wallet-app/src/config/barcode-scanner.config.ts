// Ionic Native
import { BarcodeScannerOptions } from '@ionic-native/barcode-scanner';

/**
 * Constant with the barcode plugin configuration.
 */
export const barcodeScannerOptions: BarcodeScannerOptions = {
    formats: 'QR_CODE'
};
